/**
 * Recursively create a directory under the OS's temp path.
 *
 * @param p_path Directory to create including filename
 * @returns Absolute path
 */
export declare function getTempFilePath(p_path: string): string;
